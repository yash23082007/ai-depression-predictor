{
  "name": "ai-depression-predictor",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "install-client": "cd client && npm install",
    "build": "cd client && npm install --include=dev && CI=false npm run build",
    "start": "cd client && npm run start"
  },
  "engines": {
    "node": "20.x"
  }
}
